name: My First workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My Github Actions Job"

            - name: Checkout Repo
              uses: actions/checkout@v6

            - name: List files
              run: ls

            - name: Read file
              run: cat README.md


